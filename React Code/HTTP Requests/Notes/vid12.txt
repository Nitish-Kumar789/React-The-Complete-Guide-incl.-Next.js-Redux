fetch is not only used for getting data but also to send data 
by default it will get data 
for sending 
template is like 
fetch is promise 
promise is object that respresent some value
and to resolve that value we will use await 
const response = await fetch("https://www.localhost:3000", {
    method : "PUT"
    body: JSON.stringify({places : places }),
    headers : {
        "Content-Type" : "application/json"

    }

})



so few things to note: 
while sending data since we have data ie. places in array format 
but it will travel in json format 

const data = JSON.stringify(places); 
and also we need to mention that data type while sending data 

and also all data will be stored in some json file 
name user-places.json


app.put('/user-places', async (req, res) => {
  const places = req.body.places;

  await fs.writeFile('./data/user-places.json', JSON.stringify(places));

  res.status(200).json({ message: 'User places updated!' });
});

by looking of endpoint definition 
we can note that 
we need to send object of json 
{places : places }

fetch will put data in json file

❌ Not exactly.

fetch does NOT put data in file.

👉 fetch only sends request
👉 Backend receives request
👉 Backend writes file using fs.writeFile


and since its promise and we will resolve that using 
so what we are getting in return 
"User places updated!" this message 

and also the while sending 


