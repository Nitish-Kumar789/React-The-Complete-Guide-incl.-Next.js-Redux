since async and await are so commonly used in industry so we will be using that only 

Notes: 
we will be use async and await inside useEffect only

useEffect(() => {
    async function handleData()
    {

    const response = await fetch("https://www.localhost:3000/places");
    const resData = await response.json();
    setAvailablePlace(resData.places);
    }
    handleData();

} , [])